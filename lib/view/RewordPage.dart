import 'package:flutter/material.dart';

class RewordPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Icon(Icons.cloud)));
  }
}